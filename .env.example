# Environment template for local development
# Copy this to .env and update values as needed

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=master
KEYCLOAK_CLIENT_ID=basket-stats
KEYCLOAK_CLIENT_SECRET=your-secret-here

# Firestore
GOOGLE_CLOUD_PROJECT=basket-stats-dev
FIRESTORE_EMULATOR_HOST=localhost:8081

# Google Cloud Storage
GCS_ENDPOINT=http://localhost:4443
GCS_PROJECT_ID=basket-stats-dev
GCS_BUCKET=basket-stats-dev

# Database
# (Using Firestore - no SQL database needed for MVP)
FIRESTORE_EMULATOR_HOST=localhost:8081
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001
